# Auth Connection Debug Plan

## Root Causes Found

1. **In-memory auth store loses token on page refresh** (`authStore.js` uses `let accessToken = null` — cleared on every reload). After refresh, API calls have no `Authorization` header.
2. **No auth guards on protected layouts** (`AppLayout`, `DoctorLayout`, `PatientLayout` don't check authentication). Unauthenticated users can access dashboard pages, which immediately fire API calls.
3. **No app-level auth restoration** — On page reload, the app never calls `/auth/me` to restore the user session from a stored token.
4. **Backend `JwtAuthFilter` passes through on missing/invalid tokens** instead of returning 401, allowing Spring Security's authorization rules to reject with 403.
5. **No `AuthenticationException` / `AccessDeniedException` handlers** in `GlobalExceptionHandler` to return proper JSON 401/403 responses.

## Steps to Fix

### Backend

- [x] 1. Fix `JwtAuthFilter.java` — return 401 JSON when token is missing/invalid
- [x] 2. Update `GlobalExceptionHandler.java` — add handlers for `AuthenticationException` and `AccessDeniedException`
- [x] 3. Update `SecurityConfig.java` — register custom `authenticationEntryPoint` and `accessDeniedHandler`

### Frontend

- [x] 4. Fix `authStore.js` — persist token and user in `localStorage`
- [x] 5. Update `axios.js` — handle 401/403 with redirect to login
- [x] 6. Update `App.jsx` — add `AuthInit` wrapper to restore session on mount + `RequireAuth` route guards
- [x] 7. Update `AppLayout.jsx` — add auth guard
- [x] 8. Update `DoctorLayout.jsx` — add auth guard + role check
- [x] 9. Update `PatientLayout.jsx` — add auth guard + role check

### Verification

- [ ] 10. Compile backend and test login with seeded credentials
- [ ] 11. Verify page refresh preserves auth state
- [ ] 12. Verify unauthenticated access redirects to login
